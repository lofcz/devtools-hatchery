{
  "name": "devtools-hatchery",
  "private": true,
  "workspaces": ["extensions/*", "packages/*"],
  "scripts": {
    "build": "bun run --filter '*' build"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "bun-types": "^1.2.5"
  }
}
